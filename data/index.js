const userData = require("./users");
const projectData = require("./projects");
const containerData = require("./containers");

module.exports = {
    users: userData,
    projects: projectData,
    containers: containerData
};
